module.exports = {
	name: 'tea',
	description: '',
	args: false,
	execute(message, args) {
		// make an array with 6 toppings
		// make an array with 6 teas

		// pick a random number between 1-6
		// pick a random number between 1-6 again
		// use num1 to pick topping
		// use num2 to pick tea
		// combine the strings together to get order
		// message.reply(message); // response with the order
	},
};